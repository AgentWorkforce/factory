{
  "name": "@agent-relay/factory",
  "version": "0.0.0",
  "description": "Autonomous Linear-issue → triage → PR factory. Placeholder reservation; real package lands via the p4 extraction (pear/packages/factory-sdk → here).",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AgentWorkforce/factory.git"
  },
  "homepage": "https://github.com/AgentWorkforce/factory#readme",
  "bugs": {
    "url": "https://github.com/AgentWorkforce/factory/issues"
  },
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json || true",
    "test": "true",
    "factory-build": "factory-build/run-factory-build.sh"
  },
  "devDependencies": {
    "@relayflows/cli": "^1.0.2",
    "@relayflows/core": "^1.0.2"
  }
}
