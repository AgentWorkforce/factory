// @agent-relay/factory — placeholder reservation.
//
// The real package (orchestrator, triage, github merge-gate, fleet, state,
// config) lands here via the p4 extraction of pear/packages/factory-sdk.
// Until then this stub reserves the npm name and lets the publish workflow
// + trusted-publisher setup land ahead of the real first release (0.1.0).
export {};
